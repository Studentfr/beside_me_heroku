import React from "react";

import styles from "./css/CreateFormHeader.module.css";

const CreateFormHeader = () => {
  return (
    <header className={styles.header}>
      <h2>Create Event</h2>
    </header>
  );
};

export default CreateFormHeader;
